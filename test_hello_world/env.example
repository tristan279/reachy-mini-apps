# AWS Credentials for Polly (TTS) and Transcribe (STT)
# Get these from AWS IAM Console: https://console.aws.amazon.com/iam/
# These are environment variables - set them in your shell or system environment

# Required: AWS Access Key ID
export AWS_ACCESS_KEY_ID=your_aws_access_key_id_here

# Required: AWS Secret Access Key
export AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here

# Optional: AWS Region (default: eu-west-1)
export AWS_REGION=eu-west-1

# Optional: AWS Profile name (if you used 'aws configure --profile <name>')
# Example: if you ran 'aws configure --profile reachy-mini', set this to 'reachy-mini'
export AWS_PROFILE=reachy-mini

# Optional: S3 Bucket for Transcribe (required for file-based transcription)
# AWS Transcribe requires audio files to be stored in S3
export AWS_S3_BUCKET=your-s3-bucket-name

# Optional: S3 Key Prefix for Transcribe uploads (default: transcribe/)
export AWS_S3_KEY_PREFIX=transcribe/

# Instructions:
# 1. Set these environment variables in your shell:
#    export AWS_ACCESS_KEY_ID=your_key
#    export AWS_SECRET_ACCESS_KEY=your_secret
#    etc.
# 2. Or set them in your system environment (systemd, docker, etc.)
# 3. For Transcribe, create an S3 bucket and set AWS_S3_BUCKET
# 4. The app uses os.getenv() to read these variables

